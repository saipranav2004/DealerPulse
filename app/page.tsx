// Placeholder route. The UI is deliberately not built in this session —
// scope is the data and analytics layers only. Next.js requires an app route
// for `next build` to succeed, so this stays intentionally bare.
export default function Home() {
  return <main>DealerPulse</main>;
}
